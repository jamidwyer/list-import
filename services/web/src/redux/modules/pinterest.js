// Actions
const FETCH_PINTEREST = 'FETCH_PINTEREST';

// Reducer
export default function reducer(state = {}, action = {}) {
  switch (action.type) {
    default: return state;
  }
}

// Action creators
export function fetchPinterest(username, board) {
  return
     {     
        type: FETCH_PINTEREST,
        list
     }
}